# transformations

- Stored procedures (`stored_procs/`) load from external/staging into esc_ops tables and refresh marts.
- Views (`views/`) expose curated metrics for dashboards (aging, workload, bounce, ownership timelines).
